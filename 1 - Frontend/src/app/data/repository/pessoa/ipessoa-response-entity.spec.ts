import { IPessoaResponseEntity } from './ipessoa-response-entity';

describe('IPessoaResponseEntity', () => {
  it('should create an instance', () => {
    expect(new IPessoaResponseEntity()).toBeTruthy();
  });
});
