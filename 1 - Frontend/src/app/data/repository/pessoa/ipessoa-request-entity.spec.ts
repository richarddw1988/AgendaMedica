import { IPessoaRequestEntity } from './ipessoa-request-entity';

describe('IPessoaRequestEntity', () => {
  it('deve ser criado uma instancia', () => {
    expect(new IPessoaRequestEntity()).toBeTruthy();
  });
});
