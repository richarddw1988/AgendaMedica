import { IConsultaResponseEntity } from './iconsulta-response-entity';

describe('IConsultaResponseEntity', () => {
  it('should create an instance', () => {
    expect(new IConsultaResponseEntity()).toBeTruthy();
  });
});
